<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>LLM News Aggregator</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans">
    <div class="container mx-auto p-4">
        <h1 class="text-4xl font-bold my-6 text-center text-gray-800">Latest LLM News</h1>

        <div class="bg-white p-4 rounded-lg shadow-md mb-8 flex items-center justify-center">
            <form action="/search" method="GET" class="w-full max-w-md">
                <div class="flex">
                    <input
                        class="w-full px-4 py-2 rounded-l-lg border border-gray-300 focus:outline-none focus:border-blue-500"
                        name="search"
                        type="search"
                        placeholder="Search for any other topic..."
                        aria-label="Search"
                        required
                    />
                    <button class="px-4 py-2 bg-blue-500 text-white rounded-r-lg hover:bg-blue-600 focus:outline-none" type="submit">
                        Search
                    </button>
                </div>
            </form>
        </div>

        <div id="news-container" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% if (news && news.length > 0) { %>
                <% news.forEach(article => { %>
                    <div class="article bg-white rounded-lg shadow-lg overflow-hidden flex flex-col transform hover:-translate-y-1 transition-transform duration-300">
                        <img 
                            src="<%= article.image_url ? article.image_url : 'https://via.placeholder.com/400x250.png?text=No+Image+Available' %>" 
                            alt="Article Image" 
                            class="w-full h-48 object-cover"
                        >
                        <div class="p-4 flex flex-col flex-grow">
                            <h2 class="text-xl font-bold mb-2 text-gray-900"><%= article.title %></h2>
                            <p class="text-gray-700 text-sm flex-grow">
                                <%= article.description ? article.description.substring(0, 150) + '...' : 'No description available.' %>
                            </p>
                            <p class="text-gray-500 text-xs mt-4">
                                Published on: <%= article.pubDate ? new Date(article.pubDate).toLocaleDateString() : 'N/A' %>
                            </p>
                            <a href="<%= article.link %>" target="_blank" class="text-blue-600 font-semibold mt-2 inline-block hover:underline">
                                Read more →
                            </a>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="col-span-full text-center py-10">
                    <p class="text-gray-500 text-lg">No news articles found on this topic.</p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>